<h1>Bienvenue sur The Gossip Project 🤪 !</h1>
<br>
<h3>Voici la liste des potins :</h3>

<table>
  <tr>
    <th>Nom</th>
    <th>Potin</th>
    <th>Page</th>
  </tr>
  <% @all_gossips.each do |gossip| %>
    <tr>
      <td><%= gossip.author %></td>
      <td><%= gossip.content %></td>
      <td><a href="potin/<%= gossip.id %>">Voir plus</a></td>
    </tr>
  <% end %>
</table>
